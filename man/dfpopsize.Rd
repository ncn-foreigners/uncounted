% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{dfpopsize}
\alias{dfpopsize}
\alias{dfpopsize.uncounted}
\title{Leave-One-Out Influence on Population Size}
\usage{
dfpopsize(model, ...)

\method{dfpopsize}{uncounted}(model, by = c("obs", "country"), ...)
}
\arguments{
\item{model}{A fitted \code{uncounted} object.}

\item{...}{Additional arguments passed to \code{\link{loo}}.}

\item{by}{Character: \code{"obs"} (default) or \code{"country"}.}
}
\value{
A named numeric vector. Each element is the change in \eqn{\hat\xi}
when that unit is dropped (\eqn{\hat\xi_{(-i)} - \hat\xi}).
}
\description{
Computes the change in the total estimated population size
\eqn{\hat\xi = \sum_i N_i^{\hat\alpha_i}} when each observation (or country)
is dropped. This is a convenience wrapper around \code{\link{loo}}.
}
\examples{
data(irregular_migration)
d <- irregular_migration[irregular_migration$year == 2019 & irregular_migration$sex == "m", ]
fit <- estimate_hidden_pop(d, ~ m, ~ n, ~ N, method = "poisson",
                           gamma = 0.001, countries = ~ country_code)
dp <- dfpopsize(fit)
head(dp)

}
\seealso{
\code{\link{loo}}, \code{\link{dfbeta.uncounted}}
}
