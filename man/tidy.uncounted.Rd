% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom_methods.R
\name{tidy.uncounted}
\alias{tidy.uncounted}
\title{Tidy an uncounted model}
\usage{
\method{tidy}{uncounted}(x, conf.int = FALSE, conf.level = 0.95, ...)
}
\arguments{
\item{x}{An \code{uncounted} object.}

\item{conf.int}{Logical; include confidence intervals? Default FALSE.}

\item{conf.level}{Confidence level for intervals (default 0.95).}

\item{...}{Additional arguments (ignored).}
}
\value{
A data frame with columns: \code{term}, \code{estimate},
\code{std.error}, \code{statistic}, \code{p.value}, and optionally
\code{conf.low}, \code{conf.high}.
}
\description{
Extracts a tidy coefficient table from a fitted \code{uncounted} object.
Compatible with \code{modelsummary::modelsummary()}.
}
\examples{
data(irregular_migration)
d <- irregular_migration[irregular_migration$year == "2019", ]
fit <- estimate_hidden_pop(d, ~ m, ~ n, ~ N, method = "poisson",
                           gamma = 0.005)
tidy(fit)
tidy(fit, conf.int = TRUE)

}
