% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{dfbeta.uncounted}
\alias{dfbeta.uncounted}
\title{Leave-One-Out Influence on Coefficients}
\usage{
\method{dfbeta}{uncounted}(model, by = c("obs", "country"), ...)
}
\arguments{
\item{model}{A fitted \code{uncounted} object.}

\item{by}{Character: \code{"obs"} (default) drops one observation at a time,
\code{"country"} drops all observations for one country at a time
(requires \code{countries} in the original fit).}

\item{...}{Additional arguments passed to \code{\link{loo}}.}
}
\value{
A matrix with one row per dropped unit and one column per coefficient.
Each entry is the change in the coefficient estimate relative to the full
model (\eqn{\hat\beta_{(-i)} - \hat\beta}).
}
\description{
Computes the change in estimated coefficients when each observation (or
country) is dropped. This is a convenience wrapper around \code{\link{loo}}.
}
\examples{
data(irregular_migration)
d <- irregular_migration[irregular_migration$year == "2019" & irregular_migration$sex == "Male", ]
fit <- estimate_hidden_pop(d, ~ m, ~ n, ~ N, method = "poisson",
                           gamma = 0.001, countries = ~ country_code)
db <- dfbeta(fit)
head(db)

}
\seealso{
\code{\link{loo}}, \code{\link{dfpopsize}}
}
