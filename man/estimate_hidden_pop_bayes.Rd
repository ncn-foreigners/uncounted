% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes.R
\name{estimate_hidden_pop_bayes}
\alias{estimate_hidden_pop_bayes}
\title{Bayesian Hidden-Population Models via brms}
\usage{
estimate_hidden_pop_bayes(
  data,
  observed,
  auxiliary,
  reference_pop,
  method = c("poisson", "nb"),
  cov_alpha = NULL,
  cov_beta = NULL,
  gamma = "estimate",
  cov_gamma = NULL,
  gamma_bounds = c(1e-10, 0.5),
  link_rho = c("power", "cloglog", "logit", "probit"),
  constrained = FALSE,
  weights = NULL,
  countries = NULL,
  seed = NULL,
  chains = 4,
  iter = 2000,
  warmup = floor(iter/2),
  cores = getOption("mc.cores", 1L),
  control = NULL,
  prior = NULL,
  backend = c("rstan", "cmdstanr"),
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{observed}{One-sided formula naming the observed count.}

\item{auxiliary}{One-sided formula naming the auxiliary count.}

\item{reference_pop}{One-sided formula naming the reference population.}

\item{method}{Count likelihood. Currently \code{"poisson"} and \code{"nb"}
are supported.}

\item{cov_alpha}{Optional one-sided formula for alpha covariates.}

\item{cov_beta}{Optional one-sided formula for beta covariates.}

\item{gamma}{Numeric fixed gamma, \code{"estimate"} for one bounded scalar
gamma, or \code{NULL} to use \eqn{n_i / N_i} directly.}

\item{cov_gamma}{Reserved for a future release. It must be \code{NULL}.}

\item{gamma_bounds}{Length-two numeric bounds used when
\code{gamma = "estimate"}.}

\item{link_rho}{Detection link: \code{"power"}, \code{"cloglog"},
\code{"logit"}, or \code{"probit"}.}

\item{constrained}{Logical. If \code{TRUE}, alpha is mapped through an
inverse-logit transform and beta through an exponential transform.}

\item{weights}{Optional numeric observation weights.}

\item{countries}{Optional one-sided formula for a country/group label stored
on the fitted object.}

\item{seed, chains, iter, warmup, cores, control}{Passed to \code{brms::brm()}.}

\item{prior}{Optional \pkg{brms} prior specification. If \code{NULL}, weakly
informative defaults from \code{default_priors_uncounted_bayes()} are used.}

\item{backend}{Stan backend passed to \code{brms::brm()}. Defaults to
\code{"rstan"}; \code{"cmdstanr"} is available when installed.}

\item{...}{Additional arguments passed to \code{brms::brm()}.}
}
\value{
An object of class \code{"uncounted_bayes"} containing the
\code{brmsfit} and the design metadata needed by \code{\link{popsize}}.
}
\description{
Fit Bayesian Poisson or negative-binomial hidden-population models using
\pkg{brms}. The model mirrors the count-model part of
\code{\link{estimate_hidden_pop}}:
\deqn{\log(\mu_i) = \alpha_i \log(N_i) + \log\rho_i,}
where \eqn{\rho_i = h(\beta_i \log(\gamma_i + n_i / N_i))}. Posterior
draws of \eqn{\alpha_i} are propagated to population-size draws
\eqn{\xi = \sum_i N_i^{\alpha_i}} by \code{\link{popsize}}.
}
\examples{
\dontrun{
data(irregular_migration)
d <- irregular_migration
d$ukr <- as.integer(d$country_code == "UKR")

fit_bayes <- estimate_hidden_pop_bayes(
  data = d, observed = ~m, auxiliary = ~n, reference_pop = ~N,
  method = "poisson",
  cov_alpha = ~ year * ukr + sex,
  cov_beta = ~ year,
  gamma = 0.005,
  chains = 2, iter = 1000, seed = 1
)

ps <- popsize(fit_bayes, by = ~ year + country_code, total = TRUE)
hypotheses_popsize(
  ps,
  "xi[year == 2024 & country_code == 'UKR'] < 15000"
)
}
}
