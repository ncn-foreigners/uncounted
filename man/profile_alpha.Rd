% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_uncounted.R
\name{profile_alpha}
\alias{profile_alpha}
\title{Profile Likelihood for Alpha Coefficients}
\usage{
profile_alpha(
  object,
  coef_index = 1,
  grid = NULL,
  reoptimize = FALSE,
  plot = TRUE,
  ...
)
}
\arguments{
\item{object}{An \code{"uncounted"} object.}

\item{coef_index}{Integer: which alpha coefficient to profile (1 = intercept).}

\item{grid}{Numeric vector of values to evaluate. If \code{NULL} (default),
auto-generates 30 points spanning \eqn{\pm 3} SE around the MLE.}

\item{reoptimize}{Logical. If \code{TRUE}, optimize all other parameters at
each grid point (true profile likelihood). If \code{FALSE} (default), hold
everything else at the MLE (concentrated profile, faster).}

\item{plot}{Logical; produce the plot? Default \code{TRUE}.}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
Invisibly, a data frame with columns: \code{value}, \code{xi},
\code{loglik}.
}
\description{
Evaluates the log-likelihood and population size over a grid of values
for a chosen alpha coefficient, holding other parameters at their MLE
(concentrated profile) or re-optimizing them (true profile).
}
\seealso{
\code{\link{profile_beta}}, \code{\link{profile_gamma}},
\code{\link{profile.uncounted}}
}
