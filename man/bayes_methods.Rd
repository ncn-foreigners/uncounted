% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes.R
\name{bayes_methods}
\alias{summary.uncounted_bayes}
\alias{coef.uncounted_bayes}
\alias{fitted.uncounted_bayes}
\alias{predict.uncounted_bayes}
\alias{residuals.uncounted_bayes}
\alias{loo.uncounted_bayes}
\alias{tidy.uncounted_bayes}
\alias{glance.uncounted_bayes}
\title{Methods for Bayesian Hidden-Population Models}
\usage{
\method{summary}{uncounted_bayes}(
  object,
  total = FALSE,
  level = 0.95,
  diagnostics = TRUE,
  ...
)

\method{coef}{uncounted_bayes}(object, summary = c("median", "mean", "draws"), ...)

\method{fitted}{uncounted_bayes}(object, summary = c("mean", "median", "draws"), ...)

\method{predict}{uncounted_bayes}(
  object,
  newdata = NULL,
  type = c("response", "link", "counts", "draws"),
  summary = TRUE,
  ...
)

\method{residuals}{uncounted_bayes}(
  object,
  type = c("response", "pearson"),
  summary = c("mean", "median"),
  ...
)

\method{loo}{uncounted_bayes}(object, ...)

\method{tidy}{uncounted_bayes}(x, conf.int = FALSE, conf.level = 0.95, ...)

\method{glance}{uncounted_bayes}(x, loo = FALSE, ...)
}
\arguments{
\item{object}{An \code{"uncounted_bayes"} object.}

\item{total}{Logical; include a total population-size summary in
\code{summary()}.}

\item{level}{Credible interval level for summaries.}

\item{diagnostics}{Logical; include MCMC diagnostics in \code{summary()}.}

\item{summary}{Posterior summary to return. For \code{coef()} and
\code{fitted()}, use \code{"mean"}, \code{"median"}, or \code{"draws"}.
For \code{predict()}, use \code{TRUE}/\code{"mean"}, \code{"median"}, or
\code{FALSE} to return draws. For \code{residuals()}, use \code{"mean"} or
\code{"median"} fitted values.}

\item{newdata}{Optional data frame for prediction. If \code{NULL}, predictions
are for the original data.}

\item{type}{Prediction or residual type. For \code{predict()},
\code{"response"} returns posterior expected observed counts,
\code{"link"} returns the log mean, \code{"counts"} returns replicated
posterior predictive counts, and \code{"draws"} returns response-scale
expected-count draws. For \code{residuals()}, \code{"response"} and
\code{"pearson"} are supported.}

\item{x}{An \code{"uncounted_bayes"} object for broom-style methods.}

\item{conf.int}{Logical; include credible intervals in \code{tidy()}.}

\item{conf.level}{Credible interval level for \code{tidy()}.}

\item{loo}{Logical; if \code{TRUE}, \code{glance()} also computes
\code{brms::loo()} summaries.}

\item{...}{Additional arguments passed to the underlying \pkg{brms} posterior
prediction or LOO functions where relevant.}
}
\value{
\code{summary()} invisibly returns a list containing coefficient,
population-size, and diagnostic summaries. \code{coef()} returns posterior
coefficient summaries or draws. \code{fitted()} and \code{predict()} return
numeric summaries or draw matrices. \code{residuals()} returns numeric
residuals. \code{loo()} returns the object from \code{brms::loo()}.
\code{tidy()} and \code{glance()} return data frames.
}
\description{
S3 methods for Bayesian hidden-population models fitted with
\code{\link{estimate_hidden_pop_bayes}}.
}
\details{
These methods report posterior quantities rather than Wald standard errors and
p values. Coefficient tables include posterior means, medians, posterior
standard deviations, credible intervals, R-hat, bulk ESS, and tail ESS.
Prediction methods delegate to the underlying \pkg{brms} posterior prediction
functions.
}
\examples{
\dontrun{
summary(fit_bayes, total = TRUE)
coef(fit_bayes)
head(fitted(fit_bayes))
head(predict(fit_bayes, type = "counts", summary = FALSE))
head(residuals(fit_bayes))
tidy(fit_bayes, conf.int = TRUE)
glance(fit_bayes)
loo(fit_bayes)
}
}
